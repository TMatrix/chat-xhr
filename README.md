# chat-xhr
General chat application on XMLHttpRequest, polling web-transport.

#### Start project:
1. Install dependencies:
```npm install```
2. Start server:
```npm start```

#### Enter chat:
Open browser tab on:
```localhost:5000```
